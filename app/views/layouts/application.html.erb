<!DOCTYPE html>
<html>
<head>
  <title>Espn</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
</head>
<body>
  <nav class="nav navbar navbar-default">
    <div class="container">
      <div vlass="navbar-header">
       <p class="text-lg-left"><%= link_to "Espn Fan Forum", root_path, class: "navbar-brand" %></p>
      </div>
    <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-info btn-sm" %></li>
         <% else %>
              <li><%= link_to "Log In", new_user_session_path, class: "btn btn-warning" %></li>&nbsp;
              <li><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success" %></li>
         <% end %>
       </ul>
       <% if user_signed_in? %>
         <p><%= link_to "Post It", new_message_path, class: "btn btn-success btn-lg" %></p>
         <% end %>
         <div/>
       </nav>
        <p class="notice"><%= notice %></p>
         <p class="alert"><%= alert %></p>
      <div class="container">
    <%= yield %>
    </div>
  </body>
</html>
